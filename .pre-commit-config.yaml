repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.10
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(anjor|tests)/
      - id: ruff-format
        files: ^(anjor|tests)/

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: debug-statements
        language_version: python3

  - repo: local
    hooks:
      - id: no-internal-files
        name: block internal/sensitive files from public repo
        entry: scripts/check_no_internal_files.sh
        language: script
        pass_filenames: false
        always_run: true

      - id: mypy
        name: mypy
        entry: .venv/bin/mypy
        language: system
        types: [python]
        args: [anjor/]
        pass_filenames: false
        always_run: true
